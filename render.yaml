services:
  - type: web
    name: moom-app-frontend
    env: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    redirects:
      - source: "/*"
        destination: "/index.html"
        status: 200
